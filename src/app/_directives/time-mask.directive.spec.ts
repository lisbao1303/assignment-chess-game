import { TimeMaskDirective } from './time-mask.directive';

describe('TimeMaskDirective', () => {
  it('should create an instance', () => {
    const directive = new TimeMaskDirective();
    expect(directive).toBeTruthy();
  });
});
